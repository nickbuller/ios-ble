# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'app-ble' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for app-ble
  pod 'DataDecoder'

  target 'app-bleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'app-bleUITests' do
    # Pods for testing
  end

end
